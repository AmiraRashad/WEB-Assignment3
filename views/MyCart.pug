extends layout

block content
  h1 My Cart
  -var total=0;
    table.table
      thead
        th Name
        th Price
      tbody
       each item in MyCart
         - total += Number(item.price)
         tr
            td=item.name
            td=item.price
      tfoot
        tr
          td Total
          td=total
    